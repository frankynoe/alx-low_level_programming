#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
int chiffre = 0;
while(chiffre < 10)
{
printf("%d", chiffre);
chiffre++;
}
printf("\n");
return (0);
}
